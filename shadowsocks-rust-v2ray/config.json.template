{
    "server": "0.0.0.0",
    "server_port": 8008,
    "password": "${PASSWORD}",
    "method": "${METHOD}",
    "dns": "${DNS}",
    "mode": "tcp_only",
    "plugin":"v2ray-plugin",
    "plugin_opts":"server;tls;path=/${V2RAY_PATH};host=${DOMAIN};cert=${V2RAY_CERT_PATH};key=${V2RAY_KEY_PATH};loglevel=none"
} 
